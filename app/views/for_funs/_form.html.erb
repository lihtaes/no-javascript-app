<%= form_for(for_fun) do |f| %>
  <% if for_fun.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(for_fun.errors.count, "error") %> prohibited this for_fun from being saved:</h2>

      <ul>
      <% for_fun.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
